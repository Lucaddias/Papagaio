import Foundation

// Implementações vazias remanescentes do Passo 1. Cada uma lança
// `NotImplemented` e some quando o passo indicado a substitui.
//
// Já substituídas: `WhisperEngine` (Passo 4, Transcricao/WhisperEngine.swift) e
// `QwenEngine` (Passo 7, Sumarizacao/QwenEngine.swift) e `SwiftDataRepository`
// (Passo 8, Persistencia/SwiftDataRepository.swift).
